import React from 'react';
import {StarsCIcon} from './MageIcon';

/** The studio's sparkle mark: Mage Stars C. */
export const Sparkle: React.FC<{className?: string; style?: React.CSSProperties}> = ({
  className,
  style,
}) => <StarsCIcon className={className} style={style} />;
